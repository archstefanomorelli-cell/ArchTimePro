# Arch Time Mini Timer

App desktop Windows per usare il timer di Arch Time Pro fuori dal browser.

## Sviluppo

```powershell
npm.cmd install
npm.cmd run start
```

## Build installer

```powershell
npm.cmd run dist
```

L'installer viene generato in `desktop-timer/dist`.
